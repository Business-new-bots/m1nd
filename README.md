# M1ND Telegram Bot

Telegram бот с интеграцией LLM (Groq API) для ответов на вопросы пользователей.

## 🚀 Возможности

- ✅ Регистрация пользователей при первом запуске
- ✅ Сохранение данных пользователей в `users.json`
- ✅ Интеграция с Groq API для ответов на вопросы
- ✅ История диалогов для контекстных ответов
- ✅ Подсчет количества заданных вопросов

## 📋 Требования

- Java 17+
- Maven 3.6+
- Telegram Bot Token (от [@BotFather](https://t.me/BotFather))
- Groq API Key (от [console.groq.com](https://console.groq.com))

## ⚙️ Настройка

### 1. Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### 2. Получение Groq API Key

1. Зарегистрируйтесь на [console.groq.com](https://console.groq.com)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Сохраните ключ (бесплатный tier: 30 запросов/минуту)

### 3. Конфигурация приложения

Откройте `src/main/resources/application.yaml` и заполните:

```yaml
telegram:
  bot:
    token: YOUR_BOT_TOKEN_HERE        # Токен от BotFather
    username: YOUR_BOT_USERNAME       # Username бота (без @)

llm:
  api:
    provider: groq
    groq:
      api-key: YOUR_GROQ_API_KEY      # API ключ от Groq
      model: llama-3.1-8b-instant      # Модель (можно изменить)
      url: https://api.groq.com/openai/v1/chat/completions
      temperature: 0.7                 # Креативность (0.0-1.0)
      max-tokens: 500                  # Максимальная длина ответа
```

## 🏃 Запуск

### Локальный запуск

#### Сборка проекта

```bash
mvn clean package
```

#### Запуск приложения

```bash
mvn spring-boot:run
```

Или запустите JAR файл:

```bash
java -jar target/m1nd-0.0.1-SNAPSHOT.jar
```

### 🚂 Деплой на Railway

1. **Подключите репозиторий к Railway:**
   - Создайте проект на [railway.app](https://railway.app)
   - Выберите "New Project" → "Deploy from GitHub repo"
   - Подключите ваш репозиторий

2. **Настройте переменные окружения в Railway:**
   - Перейдите в Settings → Variables
   - Добавьте следующие переменные:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен_бота
     TELEGRAM_BOT_USERNAME=имя_вашего_бота
     GROQ_API_KEY=ваш_groq_api_ключ
     PORT=8080 (обычно устанавливается автоматически)
     ```

3. **Railway автоматически:**
   - Определит Maven проект
   - Установит Java 17
   - Выполнит сборку (`mvn clean package`)
   - Запустит приложение (`java -jar target/m1nd-0.0.1-SNAPSHOT.jar`)

4. **Проверьте логи:**
   - В разделе Deployments → Logs убедитесь, что приложение запустилось
   - Должно быть сообщение "Started M1ndApplication"

**Важно:**
- Railway автоматически устанавливает переменную `PORT`, приложение настроено её использовать
- Не коммитьте токены и API ключи - используйте только переменные окружения
- Если сервис показывает "Offline", проверьте логи на наличие ошибок

## 📱 Использование

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Нажмите кнопку "Старт"
4. Задайте свой вопрос
5. Получите ответ от AI

## 📁 Структура проекта

```
src/main/java/com/example/m1nd/
├── M1ndApplication.java          # Главный класс приложения
├── bot/
│   └── M1ndTelegramBot.java      # Основной класс бота
├── config/
│   ├── TelegramBotConfig.java    # Конфигурация бота
│   └── WebClientConfig.java      # Конфигурация HTTP клиента
├── model/
│   └── User.java                 # Модель пользователя
├── service/
│   ├── UserService.java          # Сервис для работы с пользователями
│   ├── LLMService.java           # Сервис для работы с LLM API
│   └── ConversationService.java # Сервис для истории диалогов
└── util/
    └── JsonFileUtil.java         # Утилита для работы с JSON
```

## 📄 Формат users.json

Данные пользователей сохраняются в файл `users.json` в корне проекта:

```json
[
  {
    "userId": 123456789,
    "username": "username",
    "firstName": "Имя",
    "lastName": "Фамилия",
    "registeredAt": "2025-12-20T20:00:00",
    "questionsCount": 5
  }
]
```

## 🔧 Настройка LLM

### Доступные модели Groq

- `llama-3.1-8b-instant` - Быстрая модель (рекомендуется)
- `llama-3.1-70b-versatile` - Более умная, но медленнее
- `mixtral-8x7b-32768` - Большой контекст

### Параметры

- **temperature** (0.0-1.0): Креативность ответов
  - 0.0 = детерминированные ответы
  - 1.0 = креативные ответы
  - 0.7 = баланс (рекомендуется)

- **max-tokens**: Максимальная длина ответа (100-4096)

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте правильность токена в `application.yaml`
2. Убедитесь, что приложение запущено
3. Проверьте логи на наличие ошибок

### Ошибки при обращении к Groq API

1. Проверьте правильность API ключа
2. Убедитесь, что не превышен лимит запросов (30/минуту)
3. Проверьте доступность API: `https://api.groq.com`

### Файл users.json не создается

1. Проверьте права на запись в директорию проекта
2. Убедитесь, что приложение имеет доступ к файловой системе

## 📝 Логирование

Логи приложения выводятся в консоль. Уровни логирования можно настроить в `application.yaml`:

```yaml
logging:
  level:
    com.example.m1nd: INFO
    org.telegram: WARN
```

## 🔒 Безопасность

⚠️ **Важно**: Не коммитьте файлы с токенами и API ключами!

- Файл `users.json` добавлен в `.gitignore`
- Используйте переменные окружения для production:
  ```yaml
  telegram:
    bot:
      token: ${TELEGRAM_BOT_TOKEN}
  llm:
    api:
      groq:
        api-key: ${GROQ_API_KEY}
  ```

## 📚 Дополнительные ресурсы

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Groq API Documentation](https://console.groq.com/docs)
- [Spring Boot Documentation](https://spring.io/projects/spring-boot)

## 📄 Лицензия

Этот проект создан в образовательных целях.

